// import { Schema, model, models } from "mongoose";

// const userSchema = new Schema(
//   {
//     name: String,
//     email: { type: String, unique: true },
//     image: String,
//     role: { type: String, default: "user" },
//     provider: String,
//   },
//   { timestamps: true }
// );

// export const User = models.User || model("Hamed", userSchema);
